numpy>=1.24
opencv-python-headless>=4.8
flask>=3.0
pyyaml>=6.0
requests>=2.31
# optional back-ends -----------------------------------------------------
sounddevice>=0.4        # audio reactivity (needs: sudo apt install libportaudio2)
pyserial>=3.5           # Adalight serial output
python-mbedtls>=2.9     # Philips Hue Entertainment (DTLS)
spidev>=3.6 ; platform_machine in "armv7l aarch64"   # direct WS2812B on the Pi
